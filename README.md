



```bash
src/
├── config/         # Configurações (DB, env, etc.)
├── controllers/    # Regras de entrada (req -> res)
├── dtos/           # Data Transfer Objects (validações)
├── enums/          # Definição de enums
├── middlewares/    # Middlewares (erros, logs, validações)
├── models/         # Modelagem de dados (ORM)
├── repositories/   # Acesso a dados
├── routes/         # Definição das rotas
├── services/       # Regra de negócio (pura)
├── utils/          # Helpers e funções utilitárias
└── app.ts          # App principal
```